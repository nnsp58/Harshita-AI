/**
 * LegalProfileEngine
 * Manages the user's permanent legal profile details.
 */
class LegalProfileEngine {
  async getProfile(userId) {
    // Interacts with DB to fetch LegalProfile (to be added in schema.prisma)
    return {};
  }
}

module.exports = { LegalProfileEngine };
